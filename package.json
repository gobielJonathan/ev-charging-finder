{
  "name": "ev-charging-finder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "run-p type-check \"build-only {@}\" --",
    "preview": "vite preview",
    "build-only": "vite build",
    "type-check": "vue-tsc --build",
    "format": "prettier --write --experimental-cli src/",
    "cap:sync": "pnpm run build-only && npx cap sync",
    "cap:android": "pnpm run build-only && npx cap sync android && npx cap open android",
    "cap:ios": "pnpm run build-only && npx cap sync ios && npx cap open ios",
    "cap:run:android": "pnpm run build-only && npx cap sync android && npx cap run android",
    "cap:run:ios": "pnpm run build-only && npx cap sync ios && npx cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.0",
    "@capacitor/core": "^7.6.0",
    "@capacitor/ios": "^7.6.0",
    "@nosadev/vue-bottom-sheet": "^1.0.12",
    "@pinia/nuxt": "^0.11.3",
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "axios": "^1.13.6",
    "dayjs": "^1.11.19",
    "leaflet": "^1.9.4",
    "pinia": "^3.0.4",
    "vue": "^3.5.29",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.0",
    "@tsconfig/node24": "^24.0.4",
    "@types/leaflet": "^1.9.21",
    "@types/node": "^24.11.0",
    "@vitejs/plugin-vue": "^6.0.4",
    "@vue/tsconfig": "^0.8.1",
    "npm-run-all2": "^8.0.4",
    "prettier": "3.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vite-plugin-pwa": "^1.2.0",
    "vite-plugin-vue-devtools": "^8.0.6",
    "vue-tsc": "^3.2.5",
    "workbox-build": "^7.4.0",
    "workbox-window": "^7.4.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
